# Architecture — Vibe Remote

## 系統全景

```
┌──────────────────────────────────────────────────────────────┐
│                        手機 (PWA)                             │
│                                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────┐  │
│  │ Chat UI  │ │Diff View │ │Task Queue│ │ Quick Actions │  │
│  │+ Voice   │ │+ Approve │ │+ Kanban  │ │ commit/push/  │  │
│  │  Input   │ │ /Reject  │ │  Board   │ │  test/PR      │  │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └──────┬────────┘  │
│       │            │            │               │            │
│  ┌────▼────────────▼────────────▼───────────────▼────────┐  │
│  │              Services Layer                            │  │
│  │  api.ts (REST fetch) + ws.ts (WebSocket auto-reconnect)│  │
│  └──────────────────────┬────────────────────────────────┘  │
└─────────────────────────┼────────────────────────────────────┘
                          │ HTTPS + WSS
                   ┌──────┴──────┐
                   │  Tailscale  │  WireGuard encrypted tunnel
                   │  100.x.y.z  │  Zero-trust network
                   └──────┬──────┘
                          │
┌─────────────────────────┼────────────────────────────────────┐
│              Vibe Remote Server (你的 Mac / VPS / NAS)        │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                 API Gateway (Express.js)               │   │
│  │  ┌─────────┐  ┌──────────┐  ┌───────────────────┐    │   │
│  │  │  JWT    │  │   CORS   │  │   Rate Limiter    │    │   │
│  │  │  Auth   │  │  Filter  │  │   (per-device)    │    │   │
│  │  └─────────┘  └──────────┘  └───────────────────┘    │   │
│  └──────────┬──────────┬──────────┬──────────────────────┘   │
│             │          │          │                           │
│  ┌──────────▼──┐ ┌─────▼─────┐ ┌─▼───────────┐              │
│  │  AI Engine  │ │ Workspace │ │ Task Runner  │              │
│  │             │ │  Manager  │ │              │              │
│  │ Claude API  │ │ Git Ops   │ │ Queue        │              │
│  │ Context     │ │ File Tree │ │ Executor     │              │
│  │ Builder     │ │ Watcher   │ │ Branch Mgmt  │              │
│  │ Tool        │ │ Search    │ │ Dependencies │              │
│  │ Executor    │ │           │ │              │              │
│  └──────┬──────┘ └─────┬─────┘ └──────┬───────┘              │
│         │              │              │                       │
│  ┌──────▼──────────────▼──────────────▼───────────────┐      │
│  │                   SQLite Database                    │      │
│  │  conversations │ messages │ tasks │ workspaces       │      │
│  └────────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                 Workspaces (File System)               │   │
│  │  ~/projects/merak-platform/                           │   │
│  │  ~/projects/security-mcp-server/                      │   │
│  │  ~/projects/vibe-remote/                              │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

## 核心元件

### 1. API Gateway

Express.js HTTP server + WebSocket server 共享同一個 port。

```
Port 3000:
  ├── GET/POST/PUT/DELETE /api/*    → REST endpoints
  ├── WS /ws                        → WebSocket (AI streaming, real-time updates)
  └── GET /*                        → Static files (React PWA)
```

**設計決策**：
- 單一 port 部署最簡單，Tailscale 只需開一個 port
- Express 而非 Fastify/Koa — 生態最大，Claude 最熟悉，除錯最容易
- express-ws 套件處理 WebSocket，不需要獨立的 socket.io

### 2. AI Engine

負責透過 Claude Agent SDK 執行 AI 任務，是整個系統的核心。

```
AI Engine
├── claude-sdk.ts         # Claude Agent SDK wrapper
│   ├── createConversation()  # 建立新對話
│   ├── sendMessage()         # 發送訊息並處理 streaming 回應
│   ├── resumeSession()       # 使用 session ID 恢復對話（節省 token）
│   └── generateCommitMessage()   # 用 SDK 生成 commit message
│
├── context-builder.ts    # 組裝 AI context
│   ├── getProjectContext()
│   │   ├── 讀 workspace 的 file tree (filtered by .gitignore)
│   │   ├── 讀 git status + recent commits (最近 3 筆)
│   │   ├── 讀 package.json / tsconfig.json (key config files)
│   │   └── 如果 workspace 有 system prompt，加入
│   ├── getFileContext()
│   │   └── 使用者手動選擇的 context files 內容 (1MB 限制)
│   └── getConversationContext()
│       └── 最近 5 輪對話歷史（每則截斷至 2000 字元）
│
├── utils/truncate.ts     # Token 優化工具
│   ├── truncateMessage()     # 截斷單則訊息
│   ├── truncateHistory()     # 截斷對話歷史
│   └── checkFileSize()       # 檢查檔案大小
│
└── types.ts              # AI 相關 TypeScript 類型定義
```

**Token 優化策略**（已實作）：
- **Session Resume**：使用 SDK 的 `resumeSessionId` 恢復對話，減少 60-80% input tokens
- **訊息截斷**：歷史訊息限制 5 則，每則 2000 字元
- **檔案大小限制**：context files 超過 1MB 會跳過並通知使用者
- **Context Builder 優化**：減少 file tree 深度(2層)、commits(3筆)、maxFileChars(10000)

**注意**：Tool execution 由 Claude Agent SDK 內建處理，不需要自行實作 tool executor。
SDK 內建的 tools 包括：Read, Edit, Write, Bash, Grep, Glob 等。

**AI 互動流程（一次 chat message）**：

```
1. User sends message (text or voice transcription)
2. context-builder 組裝 system prompt:
   ├── Base system prompt (你是一個 coding assistant...)
   ├── Project context (file tree, git info)
   ├── User-selected file contents (1MB 限制)
   └── Workspace system prompt (if any)
3. 檢查是否有 session ID 可恢復（Session Resume）
4. 呼叫 Claude Agent SDK (`@anthropic-ai/claude-agent-sdk`)
5. 處理 streaming events → WebSocket 即時送到 client
6. SDK 內部處理 tool_use loop（Read, Edit, Bash 等），server 轉發事件
7. 完成後儲存 session ID + token usage → SQLite
8. 如果有檔案被修改 → 自動產生 diff → 推送 diff_ready event
```

### 3. Workspace Manager

管理 server 上的所有專案目錄。

```
Workspace Manager
├── manager.ts          # CRUD 操作
│   ├── register()      # 註冊一個目錄為 workspace
│   ├── list()          # 列出所有 workspaces
│   ├── getStatus()     # 取得 git status 摘要
│   └── setActive()     # 切換當前 active workspace
│
├── git-ops.ts          # Git 操作 (via simple-git)
│   ├── status()        # branch + uncommitted changes
│   ├── diff()          # staged + unstaged diff
│   ├── commit()        # commit with message
│   ├── push()          # push to remote
│   ├── pull()          # pull from remote
│   ├── checkout()      # switch/create branch
│   ├── log()           # recent commits
│   └── createPR()      # GitHub API create PR
│
├── file-tree.ts        # 檔案結構
│   ├── getTree()       # 遞迴讀取，respect .gitignore
│   └── readFile()      # 讀取單一檔案
│
├── file-watcher.ts     # chokidar 監聽
│   └── 檔案變更時 → WebSocket 通知 client 刷新
│
└── search.ts           # ripgrep wrapper
    └── search()        # 全文搜尋 codebase
```

### 4. Task Runner (Phase 2)

非同步任務執行系統。MVP 先用 in-memory queue，Phase 2 接 BullMQ。

```
Task Runner
├── queue.ts            # 任務佇列管理
│   ├── enqueue()       # 加入任務
│   ├── dequeue()       # 取出下一個任務
│   ├── getStatus()     # 查詢任務狀態
│   └── cancel()        # 取消任務
│
├── runner.ts           # 任務執行引擎
│   ├── 從 queue 取出 task
│   ├── 建立獨立 branch
│   ├── 啟動 AI 對話完成任務
│   ├── 產生 diff
│   ├── 更新狀態為 awaiting_review
│   └── 推送通知
│
├── branch-manager.ts   # 分支管理
│   ├── create()        # vibe/task-{id}-{slug}
│   ├── cleanup()       # 刪除已 merge 的分支
│   └── switch()        # 切換分支
│
└── dependency.ts       # 依賴解析
    ├── resolve()       # 拓撲排序
    └── canRun()        # 檢查前置任務是否完成
```

## 資料流

### Chat Message Flow

```
┌─────┐  text/voice   ┌─────────┐  REST POST   ┌──────────┐
│Phone│ ────────────→ │ PWA UI  │ ──────────→ │  Server  │
│     │               │         │              │          │
│     │  WS: chunks   │         │  WS: stream  │  Claude  │
│     │ ←──────────── │         │ ←────────── │   API    │
│     │               │         │              │          │
│     │  WS: diff     │         │  file change │  Tool    │
│     │ ←──────────── │         │ ←────────── │ Executor │
└─────┘               └─────────┘              └──────────┘
```

### Diff Review Flow

```
AI 修改檔案
    │
    ▼
git diff (unstaged changes)
    │
    ▼
Server 解析 diff → per-file breakdown
    │
    ▼
WebSocket push "diff_ready" → Client
    │
    ▼
User reviews file-by-file
    │
    ├─ ✅ Approve → file 標記為 approved
    ├─ ❌ Reject  → revert 該檔案的改動
    └─ 💬 Comment → 送回 AI → AI 重改 → 新 diff
    │
    ▼
All files approved?
    ├─ Yes → 啟用 commit/push 按鈕
    └─ No  → 繼續 review
```

### Task Queue Flow (Phase 2)

```
User creates task(s)
    │
    ▼
┌──────────┐    ┌─────────┐    ┌──────────────┐    ┌──────────┐
│  queued   │ →  │ running │ →  │awaiting_review│ →  │ approved │
│          │    │         │    │              │    │          │
│ 等待執行  │    │ AI 工作中│    │ 等待 user     │    │ 可 commit │
│          │    │         │    │  review diff  │    │          │
└──────────┘    └─────────┘    └──────────────┘    └──────────┘
                     │                                    │
                     ▼                                    ▼
                ┌─────────┐                        ┌──────────┐
                │ failed  │                        │committed │
                │         │                        │          │
                │ 可重試   │                        │ 已入 git  │
                └─────────┘                        └──────────┘
```

## 部署架構

### 最簡部署（推薦）

```
你的 Mac Mini / 桌機 / VPS
├── Tailscale (背景運行)
├── Vibe Remote Server (Node.js process)
│   ├── port 3000: API + PWA static files
│   └── data/: SQLite DB files
├── code-server (port 8080，桌面使用)
└── ~/projects/: 你的專案們

手機：
├── Tailscale App (背景運行)
└── Safari/Chrome → https://100.x.y.z:3000 (PWA)
```

### Docker 部署

```yaml
# docker-compose.yml
services:
  vibe-remote:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data               # SQLite 持久化
      - ~/projects:/workspaces:rw       # Mount 你的專案目錄
      - ~/.ssh:/root/.ssh:ro            # Git SSH key
      - ~/.gitconfig:/root/.gitconfig:ro
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - CLAUDE_PERMISSION_MODE=bypassPermissions
```

### 與 code-server 的共存

```
同一台 Server:
├── port 3000: Vibe Remote (手機用)
├── port 8080: code-server (電腦用)
└── ~/projects/: 共享的檔案系統

兩者完全獨立運行，但操作同一個 file system。
手機上 approve 的改動，在電腦的 code-server 上即時可見。
電腦上的改動，手機上 pull-to-refresh 也立即看到。
同步機制 = 共享 file system + git，不需要額外的 sync layer。
```

## 技術選型理由

| 選擇 | 替代方案 | 為什麼選這個 |
|-------|---------|-------------|
| Express.js | Fastify, Koa, Hono | 生態最大、AI 最熟悉、除錯容易 |
| SQLite | PostgreSQL, MongoDB | 零維護、單檔備份、個人工具夠用 |
| better-sqlite3 | sql.js, drizzle-orm | 同步 API 最簡單、效能最好 |
| React | Vue, Svelte, Solid | 生態最大、AI 產出品質最穩定 |
| Tailwind CSS | CSS Modules, styled-components | 快速迭代、不需切檔案 |
| zustand | Redux, Jotai, MobX | 最輕量、boilerplate 最少 |
| Vite | webpack, esbuild | 開發體驗最好、設定最少 |
| simple-git | nodegit, isomorphic-git | API 最直覺、維護最好 |
| zod | joi, yup, io-ts | TypeScript 原生推導、最流行 |
| Web Speech API | Whisper, Deepgram | 瀏覽器原生、零成本、零延遲 |

## 效能考量

- **AI Response**：Claude API latency 約 1-3 秒首個 token，streaming 避免使用者等待
- **SQLite**：單一寫入者，不需要 WAL mode（但建議開啟 `journal_mode=WAL` 以允許讀寫並行）
- **File Tree**：大型 monorepo 可能有上萬檔案，要設好 depth limit 和 ignore patterns
- **WebSocket**：心跳每 30 秒，自動重連策略（1s → 2s → 4s → 8s → 30s max）
- **Push Notification**：需要 VAPID key pair，server 端儲存 subscription
